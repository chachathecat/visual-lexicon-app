name: Risk Gate

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Report no-op status
        run: echo "Automated risk classification is not implemented. No risk decision is being made."
