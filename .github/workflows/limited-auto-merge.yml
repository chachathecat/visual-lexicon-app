name: Limited Auto Merge

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Report disabled status
        run: echo "Auto-merge is disabled and not yet implemented."
