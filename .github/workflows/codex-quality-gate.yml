name: Codex Quality Gate

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Report no-op status
        run: echo "Codex quality gate is a manual no-op. No production quality decision is being made."
